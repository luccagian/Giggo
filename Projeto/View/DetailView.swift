import SwiftUI

struct DetailView: View {
    let dados: InfosBasicas
    
    var body: some View {
        VStack{
            
            
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color("BlueGiggo"))
    }
}

#Preview {
    DetailView(dados: InfosBasicas.previewInfo)
}
