//
//  HomeView.swift
//  Projeto
//
//  Created by Aluno Mack on 25/05/26.
//

import SwiftUI
import UIKit
//import CalendarView

//Definir o Background depois
let backgroundDark = Color.blue

/*LinearGradient(
    colors: [Color.red, Color.blue],
    startPoint: .top, endPoint: .bottom)*/

/**
 
 
 ------------   CALENDARIO     ----------------
 
 **/
struct CalendarView: UIViewRepresentable {
    //Protocolo UIView do UIKit dentro do SwiftUI.
    
    @Binding var selectedDate: Date?

    func makeUIView(context: Context) -> UICalendarView {
        
        let view = UICalendarView()
        
        view.calendar = Calendar(identifier: .gregorian)
        view.locale = Locale(identifier: "pt_BR")
        view.fontDesign = .rounded
        view.backgroundColor = .white
        
        view.layer.borderWidth = 1
        view.layer.borderColor = UIColor.systemGray4.cgColor
        view.layer.cornerRadius = 12
        view.clipsToBounds = true//cortar tudo que vazar
        
        
        
        
        return view
    }

    func updateUIView(_ uiView: UICalendarView, context: Context) {
    }
}



struct HomeView: View {
    
    @State private var selectedDate: Date? = nil
//Data do calendario
    var body: some View {
        
        ZStack{
            backgroundDark
                .ignoresSafeArea()
            
            VStack {
                
                        
                        CalendarView(selectedDate: $selectedDate)
                    .frame(height:300)
                        
                    
                    Text("Minha lista")
                    }//VerticalStack
            
            
        }//Zstack do Backgroun nao remover!
        
    }//Body
}//Main


#Preview {
    HomeView()
}
